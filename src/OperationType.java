//used as a list for all possible operation types.
public enum OperationType {
    AND, OR, XOR, NOT, LEFT_SHIFT, RIGHT_SHIFT, ADD, SUBTRACT,
    MULTIPLY, EQUALS, NOT_EQUALS, LESS_THAN, GREATER_THAN_OR_EQUAL,
    GREATER_THAN, LESS_THAN_OR_EQUAL
}
